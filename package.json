{
  "name": "frida-analykit",
  "version": "1.0.0",
  "description": "frida分析工具",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile index.ts -o _agent.js -c",
    "watch": "frida-compile index.ts -o _agent.js -w"
  },
  "devDependencies": {
    "@types/frida-gum": "^18.7.2",
    "@types/node": "^18.19.3",
    "frida-compile": "^16.4.1"
  }
}
